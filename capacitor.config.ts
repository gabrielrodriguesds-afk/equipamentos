import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.empresa.equipamentos',
  appName: 'Sistema Equipamentos',
  webDir: 'dist/public'
};

export default config;
